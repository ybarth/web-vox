//! Native messaging host binary for browser extension communication.
//! Implements the Chrome Native Messaging protocol (length-prefixed JSON on stdin/stdout).

fn main() {
    eprintln!("web-vox native messaging host — not yet implemented");
    eprintln!("This binary will be completed in Phase 2.");
    std::process::exit(0);
}
